{
  "name": "safe-transfer",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy-dev": "npx hardhat run scripts/deploy.js --network ropsten"
  },
  "dependencies": {
    "@alch/alchemy-web3": "^1.0.3",
    "@openzeppelin/contracts": "^4.3.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "ethers": "^5.3.0",
    "hardhat": "^2.3.0"
  }
}
